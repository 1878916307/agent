# 多智能体竞品调研系统 - 面试准备指南

## 一、必须掌握的核心知识点

### 1. LangGraph 状态图（最核心）

| 概念 | 说明 | 对应代码 |
|------|------|----------|
| **State** | 所有 Agent 共享的全局数据，用 `TypedDict` 定义 | `app/agents/state.py` |
| **Reducer** | `Annotated[list[BaseMessage], add_messages]` 让消息追加而非覆盖 | `state.py` 第 36 行 |
| **条件边** | `route_after_supervisor()` 函数，根据 State 动态路由到下一个节点 | `app/graph/workflow.py` |
| **图的编译** | `StateGraph → add_node → add_edge → compile` | `workflow.py` 的 `build_graph()` |

**你需要能解释：**
- State 是怎么在 Agent 之间传递的？→ 所有节点读写同一个 State 对象
- 为什么用 `Annotated + add_messages`？→ 默认是覆盖，用 Reducer 变成追加，保证对话历史不丢失
- 条件边怎么工作的？→ 路由函数读取 State，返回下一个节点名称字符串，LangGraph 自动跳转
 
### 2. Supervisor 模式（面试高频）

```
                    ┌─────────────┐
                    │  START      │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
              ┌────►│ Supervisor  │◄────────────┐
              │     │  (总指挥)    │              │
              │     └──────┬──────┘              │
              │            │ 条件路由             │
              │     ┌──────┼──────┬──────┐       │
              │     │      │      │      │       │
              │  ┌──▼──┐ ┌▼───┐ ┌▼───┐ ┌▼────┐  │
              │  │研究  │ │分析│ │撰写│ │审核 │──┘
              │  │Agent │ │Agent│ │Agent│ │Agent│
              │  └─────┘ └────┘ └────┘ └─────┘
              │                              │
              └──────────────────────────────┘
                         (结果回流)
```

- Supervisor [supervisor.py](app/agents/supervisor.py) **不干活，只负责调度**
- 4 个专项 Agent 各司其职，完成后结果回流给 Supervisor
- 审核不通过 → 打回 Writer 修改（循环机制）

**你需要能解释：**
- Supervisor 模式 vs 去中心化模式的区别？
  - Supervisor 有总指挥统一调度，可控性强，适合生产环境
  - 去中心化是 Agent 之间直接通信，灵活但难控制
- 为什么选 Supervisor？→ 调研流程需要严格的阶段控制（采集→分析→撰写→审核）

### 3. Tool Calling（工具调用）

```
用户请求 → LLM 判断需要调用工具 → 输出 tool_calls（工具名+参数）
    → 代码手动执行工具 → 结果包装成 ToolMessage 喂回 LLM
    → LLM 根据结果继续决策（可能继续调工具 or 生成最终回答）
```

**对应代码：** `app/agents/researcher.py`
- `bind_tools([search_competitor_info, search_market_data])` 绑定工具
- LLM 返回 `tool_calls` → 循环执行 → `ToolMessage` 回传结果

**你需要能解释：**
- 为什么不直接把工具函数写在 Agent 里？→ 解耦，工具可以复用，LLM 自主决策调什么
- `bind_tools` 的原理？→ 把工具的名称、描述、参数 schema 注入到 LLM 的 system prompt 中

### 4. 幻觉抑制机制

| 策略 | 实现方式 | 代码位置 |
|------|----------|----------|
| **生成与审核分离** | Writer 写报告，Reviewer 独立审核 | `writer.py` / `reviewer.py` |
| **循环修正** | 审核不通过打回 Writer，最多 2 次 | `supervisor.py` 的 `_parse_decision()` |
| **数据溯源** | 要求 Reviewer 检查数据来源 | `reviewer.py` 的 system prompt |

### 5. 异步架构

- FastAPI 的 `POST /api/v1/research` 接收请求后立即返回 `task_id`
- 用 `run_in_executor` 在后台线程执行调研任务
- `GET /api/v1/research/{task_id}` 轮询任务状态

**对应代码：** `app/main.py`

---

## 二、简历写法

### 项目名称
多智能体竞品调研系统

### 技术栈
Python / FastAPI / LangGraph / LangChain / DeepSeek API / Tavily Search / SQLite / LangSmith

### 项目描述（简历上写 2-3 行）

> 基于 LangGraph 状态图 + Supervisor 监管者模式构建的多 Agent 协作系统。系统包含 5 个专项 Agent（总指挥、信息采集、数据分析、报告撰写、合规审核），通过条件边实现动态路由与循环修正，可自动完成竞品调研、SWOT 分析、报告生成与审核的全流程。支持 SSE 实时流式推送、SQLite 持久化、PDF 报告导出，集成 LangSmith 可观测性追踪。

### 核心亮点（面试时展开讲）

1. **架构设计**：采用 Supervisor 模式，1 个调度中心 + 4 个执行 Agent，通过 LangGraph 条件边实现动态路由
2. **状态管理**：使用 `Annotated + Reducer` 机制实现 Agent 间消息传递，State 驱动整个工作流
3. **幻觉抑制**：生成与审核分离 + 循环修正机制，Reviewer 独立审核报告质量
4. **工具调用**：Researcher Agent 通过 `bind_tools` 绑定 Tavily 联网搜索，LLM 自主决策搜索策略
5. **SSE 流式输出**：使用 LangGraph `stream()` + Server-Sent Events 实时推送每个 Agent 执行进展
6. **数据持久化**：SQLite 存储任务状态和报告，服务重启数据不丢失，支持历史查询
7. **PDF 导出**：Markdown → HTML → PDF 转换链，支持中文字体
8. **可观测性**：集成 LangSmith，可视化追踪每个 Agent 的调用链、耗时、token 消耗

---

## 三、面试常见问答

### Q1: 为什么用 LangGraph 不用 LangChain 的 AgentExecutor？

> AgentExecutor 是单 Agent 线性执行，不支持多 Agent 协作、循环、分支。LangGraph 的图结构支持条件路由、循环、中断恢复，更适合复杂工作流。而且 LangGraph 的 State 机制让 Agent 间数据传递更清晰可控。

### Q2: Supervisor 模式 vs 去中心化模式的区别？

> | 对比维度 | Supervisor 模式 | 去中心化模式 |
> |---------|----------------|-------------|
> | 控制方式 | 总指挥统一调度 | Agent 之间直接通信 |
> | 可控性 | 强，流程可预测 | 弱，行为难预测 |
> | 适用场景 | 生产环境、流程固定 | 探索性任务、头脑风暴 |
> | 扩展性 | 加 Agent 需改路由 | 加 Agent 较灵活 |

### Q3: State 的 Reducer 是干什么的？

> 默认情况下，节点返回值会**覆盖** State 同名字段。用 `Annotated[list, add_messages]` 后，新消息会**追加**到列表末尾而非覆盖，保证对话历史不丢失。这是 LangGraph 的核心机制之一。

```python
# 没有 Reducer → 覆盖
messages: list[BaseMessage]

# 有 Reducer → 追加
messages: Annotated[list[BaseMessage], add_messages]
```

### Q4: 怎么防止 Agent 死循环？

> 两个保护机制：
> 1. `iteration_count` 计数器，超过 `MAX_RESEARCH_ITERATIONS`（3次）强制结束
> 2. Reviewer 最多打回 `MAX_REVIEW_RETRIES`（2次），超过直接结束

### Q5: Tool Calling 的流程是什么？

> 1. `bind_tools` 把工具的名称、描述、参数 schema 注入 LLM
> 2. LLM 输出 `tool_calls`（包含工具名 + 参数 JSON）
> 3. 代码解析 `tool_calls`，手动执行对应工具函数
> 4. 把执行结果包装成 `ToolMessage` 追加到消息列表
> 5. LLM 根据工具结果继续决策（可能继续调工具 or 生成最终回答）

### Q6: 如果某个 Agent 出错了怎么办？

> 1. 每个 Agent 节点有 try-except 捕获异常
> 2. State 中有 `iteration_count` 防止无限循环
> 3. 路由函数有兜底逻辑，异常时可路由到 END

### Q7: 这个系统怎么扩展？

> 1. 加新 Agent：写一个新的 agent 文件 → 在 workflow.py 注册节点 → 在路由函数加条件
> 2. 加新工具：在 tools 目录加函数 → 在 researcher.py 的 bind_tools 里注册
> 3. 换 LLM：只需改 .env 配置，代码不用动（OpenAI 兼容格式）

### Q8: SSE 流式输出是怎么实现的？（新增重点）

> **核心原理：** LangGraph 的 `stream()` 方法每执行完一个节点就 yield 一次状态更新，结合 FastAPI 的 `StreamingResponse` + SSE 协议实时推送给前端。
>
> **代码流程：**
> ```python
> # main.py 中的 _run_research_task_stream()
> for event in graph.stream(initial_state):      # 每完成一个 Agent 就 yield
>     for node_name, state_update in event.items():
>         yield {"event": "agent_complete", "data": {...}}  # 推送 SSE 事件
> ```
>
> **SSE 协议格式：**
> ```
> event: agent_complete
> data: {"agent": "researcher", "message": "researcher 执行完成"}
> ```
>
> **前端接收（EventSource）：**
> ```javascript
> const source = new EventSource('/api/v1/research/abc123/stream');
> source.addEventListener('agent_complete', (e) => {
>     console.log('Agent完成:', JSON.parse(e.data));
> });
> ```

### Q9: SSE vs WebSocket 的区别？为什么选 SSE？（新增重点）

> | 对比维度 | SSE | WebSocket |
> |---------|-----|----------|
> | 通信方向 | 单向（服务端→客户端） | 双向 |
> | 协议 | 标准 HTTP | 独立协议（ws://） |
> | 复杂度 | 简单，天然支持断线重连 | 复杂，需手动处理重连 |
> | 适用场景 | 服务端推送通知/进度 | 聊天室/实时协作 |
>
> 选 SSE 的原因：调研任务是「服务端执行→前端看结果」的单向推送场景，SSE 更轻量，且天然兼容 HTTP。

### Q10: SQLite 持久化怎么做的？怎么迁移到生产数据库？（新增重点）

> **实现：** `app/db/database.py` 封装了 SQLite 操作：
> - `init_db()` → 创建 tasks 表（启动时自动调用）
> - `create_task()` → 新建任务，JSON 序列化列表字段
> - `update_task()` → 参数化更新，防 SQL 注入
> - `get_task()` / `list_tasks()` → 查询
>
> **关键设计：**
> ```python
> @contextmanager
> def get_db():
>     conn = sqlite3.connect(DB_PATH)
>     conn.row_factory = sqlite3.Row  # 支持列名访问
>     try:
>         yield conn
>     finally:
>         conn.close()  # 自动关闭，防止连接泄漏
> ```
>
> **迁移到 PostgreSQL：** 只需替换 `get_db()` 中的连接方式，SQL 语句基本兼容。也可以引入 SQLAlchemy ORM 做数据库无关化。

### Q11: LangSmith 是怎么集成的？（新增重点）

> **零代码侵入**：只需在 `.env` 中配置 `LANGSMITH_API_KEY`，LangChain 会自动检测并启用 tracing。
>
> ```python
> # llm.py 中的关键代码
> if settings.LANGSMITH_ENABLED:
>     os.environ["LANGCHAIN_TRACING_V2"] = "true"
>     os.environ["LANGCHAIN_PROJECT"] = "multi-agent-research"
> ```
>
> **LangSmith 能看到什么：**
> - 每次 LLM 调用的 prompt 和 completion
> - 每个 Agent 的执行耗时
> - Token 消耗统计
> - 工具调用的输入输出
> - 完整的调用链追踪（trace）
>
> **面试价值：** 直接打开 LangSmith 面板给面试官看，比口述有说服力。

### Q12: PDF 导出怎么实现的？（新增重点）

> **转换链：** Markdown → HTML → PDF
>
> ```python
> import markdown
> from weasyprint import HTML
>
> # 1. Markdown → HTML（支持表格、代码块、目录）
> html_content = markdown.markdown(report, extensions=["tables", "fenced_code", "toc"])
>
> # 2. 包装完整 HTML（含中文字体样式）
> full_html = f"<html><head><style>...</style></head><body>{html_content}</body></html>"
>
> # 3. HTML → PDF
> pdf_bytes = HTML(string=full_html).write_pdf()
> ```
>
> **优雅降级：** 如果 `markdown` 或 `weasyprint` 未安装，接口返回 500 并提示安装命令，不影响其他功能。

---

## 四、API 接口一览（v2.0）

| 方法 | 路径 | 说明 | 备注 |
|------|------|------|------|
| POST | `/api/v1/research` | 提交调研任务（异步） | 返回 task_id，轮询查结果 |
| POST | `/api/v1/research/{task_id}/stream` | SSE 流式调研 | 实时推送 Agent 进展 |
| GET | `/api/v1/research/{task_id}` | 查询任务状态 | 轮询模式 |
| GET | `/api/v1/research/{task_id}/export` | 导出 PDF 报告 | 报告完成后才可导出 |
| GET | `/api/v1/tasks` | 历史任务列表 | SQLite 持久化 |
| GET | `/api/v1/health` | 健康检查 | - |

---

## 五、项目文件结构速查

```
d:\document\ai\code\
├── .env                        # 环境配置（API Key、模型名、LangSmith）
├── requirements.txt            # 依赖列表
├── run.py                      # 启动入口
├── app/
│   ├── config.py               # 配置管理（读取 .env，含 LangSmith/SQLite 配置）
│   ├── main.py                 # ★ FastAPI 主应用（6个接口，含 SSE/PDF）
│   ├── db/
│   │   ├── __init__.py
│   │   └── database.py         # ★ SQLite 持久化模块
│   ├── models/
│   │   └── schemas.py          # Pydantic 数据模型
│   ├── tools/
│   │   └── search.py           # Tavily 搜索工具封装
│   ├── agents/
│   │   ├── state.py            # ★ LangGraph State 定义
│   │   ├── llm.py              # ★ LLM 工厂函数（含 LangSmith 集成）
│   │   ├── supervisor.py       # ★ 总指挥 Agent（调度中心）
│   │   ├── researcher.py       # ★ 信息采集 Agent（工具调用）
│   │   ├── analyst.py          # 数据分析 Agent
│   │   ├── writer.py           # 报告撰写 Agent
│   │   └── reviewer.py         # 合规审核 Agent
│   └── graph/
│       └── workflow.py          # ★ LangGraph 状态图构建
├── data/
│   └── research.db              # SQLite 数据库文件（自动生成）
└── tests/
    └── test_research.py         # 测试脚本（支持 --stream SSE 模式）

★ = 面试重点代码
```

---

## 六、你现在应该做的事

1. **通读每个 Agent 的代码**，理解它的输入/输出/职责
2. **跑通测试**（已完成 ✓），看服务端日志理解整个流程
3. **尝试修改**：比如换调研主题、加新 Agent、改 State 字段
4. **能用自己的话讲出来**：对着镜子讲"我这个项目是怎么工作的"
5. **准备画图**：面试时画一个 Supervisor 调度流程图，非常加分
